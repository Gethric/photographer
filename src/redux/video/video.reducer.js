import VIDEO from "./video";

const INITIAL_STATE = {
	video: VIDEO
};

const videoReducer = (state = INITIAL_STATE, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default videoReducer;
